// Simple trainer landing page - just handles mode selection
document.addEventListener('DOMContentLoaded', () => {
    // No complex initialization needed for the landing page
    console.log('Trainer landing page loaded');
});